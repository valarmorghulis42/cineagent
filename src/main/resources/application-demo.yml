# Demo profile: default H2 datasource + seeded demo data (V900__demo_data.sql) + a short hold
# TTL so the video doesn't have to wait for realistic timings.
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/demo-data

app:
  hold:
    ttl-minutes: 1
    sweep-interval: PT5S
